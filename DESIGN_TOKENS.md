# Design Tokens — Copied from user-frontend

This app's `app/globals.css` (Tailwind v4 `@theme` tokens) and font setup in `app/layout.tsx` were copied verbatim from `../user-frontend/` on **2026-08-07**. `user-frontend` is the canonical source (see its `DESIGN_TOKENS.md`).

If the tokens change upstream, re-copy `app/globals.css` from `user-frontend` and update this date. This duplication is deliberate — see the build plan for rationale.
